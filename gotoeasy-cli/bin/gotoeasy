#!/usr/bin/env node

const commander = require('commander')

commander
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('publish', 'npm publish')
  .command('p', 'npm publish')
  .parse(process.argv);
