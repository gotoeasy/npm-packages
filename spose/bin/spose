#!/usr/bin/env node

const commander = require('commander')

commander
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('eureka', 'generate jar for eureka registration server')
  .parse(process.argv);
